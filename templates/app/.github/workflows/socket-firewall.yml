name: Socket Firewall

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

jobs:
  firewall:
    name: Socket Firewall
    permissions:
      contents: read
    uses: collinbentley1/platform/.github/workflows/socket-firewall.yml@__PLATFORM_SHA__
