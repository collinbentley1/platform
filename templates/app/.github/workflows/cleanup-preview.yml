name: Cleanup preview

on:
  pull_request:
    types:
      - closed

permissions: {}

jobs:
  cleanup:
    permissions:
      id-token: write # Exchange only for the exact-SHA preview traffic operator.
    uses: collinbentley1/platform/.github/workflows/cleanup-preview.yml@__PLATFORM_SHA__
