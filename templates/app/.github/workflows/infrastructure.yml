name: Infrastructure

on:
  pull_request:

permissions: {}

jobs:
  infrastructure:
    permissions:
      contents: read
    uses: collinbentley1/platform/.github/workflows/infrastructure.yml@__PLATFORM_SHA__
