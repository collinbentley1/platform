name: Application

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

jobs:
  verify:
    name: Bun verify
    permissions:
      contents: read
    uses: collinbentley1/platform/.github/workflows/application.yml@__PLATFORM_SHA__
