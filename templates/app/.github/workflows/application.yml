name: Application

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize
  push:
    branches:
      - main

permissions: {}

jobs:
  verify:
    name: Bun verify
    permissions:
      contents: read
    uses: collinbentley1/platform/.github/workflows/application.yml@__PLATFORM_SHA__
